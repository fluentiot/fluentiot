
const Device = require('./../device');

class PirDevice extends Device {

    setup() {
        // Default attributes
        this.attributes = {
        };

        // Default capabilities
        this.capabilities = {
        };
    }
    
}

module.exports = PirDevice;
